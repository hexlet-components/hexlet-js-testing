# pnpm с 10-й версии не запускает build-скрипты зависимостей без явного
# разрешения. unrs-resolver собирает нативный модуль, без него не работает
# резолвер импортов.
allowBuilds:
  unrs-resolver: true
